---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    install:
      packages:
        - vim
        - jq
        - smartmontools

  - type: files
    files:
      - source: system
        destination: /

  - type: signing
